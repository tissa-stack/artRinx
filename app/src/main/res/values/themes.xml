<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.ArtRinx" parent="android:Theme.Material.Light.NoActionBar" />

    <style name="Theme.ArtRinx.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_logo</item>
        <!-- No icon background color: it forced the icon into a ~160dp circle that clipped the wide
             6:1 wordmark. The plate was invisible anyway (same color as the splash background). -->
        <item name="postSplashScreenTheme">@style/Theme.ArtRinx</item>
    </style>

</resources>
