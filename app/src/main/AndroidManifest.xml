<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Firebase Analytics' play-services-measurement SDK auto-merges these so it *could* read the
         Advertising ID (GAID) / run Privacy-Sandbox ad attribution. This app shows no ads and does no
         ad attribution, so strip all three. Removing AD_ID also aligns the app with our Play Console
         "no advertising ID" data-safety declaration. -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" tools:node="remove" />
    <uses-permission android:name="android.permission.ACCESS_ADSERVICES_ATTRIBUTION" tools:node="remove" />
    <uses-permission android:name="android.permission.ACCESS_ADSERVICES_AD_ID" tools:node="remove" />

    <!-- Package visibility (Android 11+): lets the share sheet enumerate apps that accept a
         text/plain ACTION_SEND, to build the custom share-target row. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.SEND" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>

    <application
        android:name=".ArtRinxApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:enableOnBackInvokedCallback="true"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ArtRinx">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.ArtRinx.Starting"
            android:windowSoftInputMode="adjustNothing"
            tools:ignore="LockedOrientationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Invite deep link (custom scheme): rinxart://invite/<CODE> -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="rinxart" android:host="invite" />
            </intent-filter>

            <!-- Invite universal link: https://www.artrinx.com/invite/<CODE> -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="www.artrinx.com"
                    android:pathPrefix="/invite" />
            </intent-filter>

            <!-- Event deep link (custom scheme): rinxart://event/<ID> -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="rinxart" android:host="event" />
            </intent-filter>

            <!-- Event universal link: https://www.artrinx.com/event/<ID> -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="www.artrinx.com"
                    android:pathPrefix="/event" />
            </intent-filter>

            <!-- Content deep links (custom scheme): rinxart://{artwork|curation|profile}/<ID> -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="rinxart" android:host="artwork" />
                <data android:scheme="rinxart" android:host="curation" />
                <data android:scheme="rinxart" android:host="profile" />
            </intent-filter>

            <!-- Content universal links: https://www.artrinx.com/{artwork|curation|profile}/<ID>.
                 These are the URLs built by ShareTarget.webUrl for the system share sheet, so a
                 tapped shared link opens the app (subject to /.well-known/assetlinks.json on the
                 host). Routing is already handled by DeepLinkParser.fromPath(). -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="www.artrinx.com" android:pathPrefix="/artwork" />
                <data android:scheme="https" android:host="www.artrinx.com" android:pathPrefix="/curation" />
                <data android:scheme="https" android:host="www.artrinx.com" android:pathPrefix="/profile" />
            </intent-filter>
        </activity>

        <!-- Firebase Cloud Messaging (API §11 / handout Push Notifications). -->
        <service
            android:name=".core.push.RinxMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="@string/default_notification_channel_id" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@mipmap/ic_launcher" />

        <!-- Explicitly stop Firebase Analytics from collecting the Advertising ID at runtime. Pairs
             with the AD_ID permission removal above: the permission strip is the mechanism Google
             documents, this flag makes the intent explicit so the SDK never probes for the GAID. -->
        <meta-data
            android:name="google_analytics_adid_collection_enabled"
            android:value="false" />
    </application>

</manifest>
